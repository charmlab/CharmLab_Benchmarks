experiment:
  name: "compas_probe_mlp_experiment"
  seed: 42
  num_factuals: 20  # how many negative-class samples to generate counterfactuals for
  factual_selection: "negative_class"  # Options: "negative_class", "all"
  output_dir: "./results"
  save_results: true
  output_format: "csv"  # Options: "csv", "json", "both"
  logger: "info"  # Options: "debug", "info", "warning", "error"

# ---------- Data Layer ----------
data:
  - name: "compas_carla"
  # Override any key inside the data config without editing it directly.
  # Keys here are merged ON TOP of whatever is in data_config_<name>.yml.
    overrides:
      train_split: 0.75
      balance_classes: false

# ---------- Model Layer ----------
model:
  name: "mlp" # Options: "mlp", "logistic_regression", etc.
  overrides:
    epochs: 25
    learning_rate: 0.002
    optimizer: "rms"
    batch_size: 25
    hidden_layers: [[50]]
    n_output: 2
    output_activation: "softmax"

# ---------- Method Layer ----------
method:
  name: "PROBE"  # Options: "ROAR", "PROBE", etc.
  overrides:
    lambda_: 0.01
    y_target: [0.45, 0.55]
    n_iter: 200
    # loss_type: "BCE"

# ---------- Evaluation Layer ----------
evaluation:
  metrics:
    - name: "Distance"
    - name: "Validity"
      # hyperparameters: {}  # Optional per-metric hyperparameters
  # - name: "Sparsity"
  # - name: "Validity"